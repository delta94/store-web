export const isChrome = (browser?: string) => browser === 'Chrome';

export const isEdge = (browser?: string) => browser === 'Microsoft Edge';

export const isFirefox = (browser?: string) => browser === 'Firefox';

export const isYandex = (browser?: string) => browser === 'Yandex Browser';

export const isOpera = (browser?: string) => browser === 'Opera';
